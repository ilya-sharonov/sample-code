export enum Status {
    Ok = 'ok',
    Error = 'error',
}

export enum Label {
    Person = 'Person',
    Activity = 'Activity',
    Group = 'Group'
}

export enum StatusCode {
    Ok = 200,
}

export enum ReasonPhrase {
    Ok = 'OK',
}

export enum Decision {
    Accept = 'accept',
    Decline = 'decline',
    Tentative = 'tentative',
}
