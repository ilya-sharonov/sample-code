export enum ActivityError {
    Create = 'ACTIVITY_CREATE_ERROR',
    Update = 'ACTIVITY_UPDATE_ERROR',
    Get = 'ACTIVITY_GET_ERROR',
}

export enum ActivityStateError {
    Update = 'ACTIVITY_STATE_UPDATE_ERROR',
}
